<div class="row">
	<div class="col-md-8">
		<h1>Popular User</h1>
		<p class="lead">Most activer users</p>
		<%= render partial: 'users/user', collection: @users %>
		<%= will_paginate @user %>
	</div>
	<div class="col-md-4">
	</div>
</div>
